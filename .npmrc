# Hoist all packages to root node_modules — required for Next.js and Prisma
# to resolve dependencies correctly in a pnpm workspace.
shamefully-hoist=true

# Do not fail on peer dependency warnings (monorepos often have version skew)
strict-peer-dependencies=false

# Auto-install peer deps
auto-install-peers=true
